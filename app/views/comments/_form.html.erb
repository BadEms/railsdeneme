<%= form_for([@post,Comment.new], :remote=>'true', :controller =>  'comments', :action => 'create') do |f| %>
<div>
  <%= f.text_field :bdy, id: :comment_bdy %>
</div>

<div class="actions">
  <%= f.submit %>
</div>

<%end%>